---
title : 'vessel'
output : html_document
---

```{r setup, include = FALSE}

knitr::opts_chunk$set(echo = TRUE)

```

# vessel.R
#
# William John Zywiec
# The George Washington University
#
# ...

```{r}

# initialize environment
if (!is.null(dev.list())) dev.off()
rm(list = ls())
cat('\014')

# load packages
library(parallel)

# set source and training directories
source.dir <- 'C:/Users/Will/Dropbox/Dissertation/R/main'
training.dir <- 'C:/Users/Will/Desktop/test'

# set variables
ensemble.size <- 5
sample.size <- 1e+06
risk.pool <- 5

# load functions
source(paste0(source.dir, '/bn.R'))
source(paste0(source.dir, '/generate.R'))
source(paste0(source.dir, '/nn.R'))
source(paste0(source.dir, '/risk.R'))

```

```{r}

# build Bayesian network
BN()

```

```{r}

# generate data
Generate(source.dir, training.dir)

```

```{r}

# build neural network
NN(ensemble.size, source.dir, training.dir)

```

```{r}

# assess risk
Risk(bn, ensemble.size, sample.size, risk.pool, source.dir, training.dir)

```
