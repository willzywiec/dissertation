---
title : 'vessel'
output : html_document
---

```{r setup, include = FALSE}

knitr::opts_chunk$set(message = FALSE, root.dir = 'E:/test-xxx', warning = FALSE)

```

# vessel.Rmd
#
# William John Zywiec
# The George Washington University
#
# ...

```{r}

# initialize environment
if (!is.null(dev.list())) dev.off()
rm(list = ls())
cat('\014')

# load packages
library(parallel)

# set source and test directories
source.dir <- 'C:/Users/Will/Dropbox/Dissertation/R/main'
test.dir <- 'E:/test-xxx'

# set variables
ensemble.size <- 1
sample.size <- 1e+06
risk.pool <- 10

# load functions

source(paste0(source.dir, '/nn.R'))
source(paste0(source.dir, '/bn.R'))
source(paste0(source.dir, '/risk.R'))

```

```{r}

# build neural network
# NN(ensemble.size, 0.00001)
NN(ensemble.size, 0.00025)
NN(ensemble.size, 0.0005)
NN(ensemble.size, 0.00075)
NN(ensemble.size, 0.001)
NN(ensemble.size, 0.00125)
NN(ensemble.size, 0.0015)
NN(ensemble.size, 0.00175)
NN(ensemble.size, 0.002)
NN(ensemble.size, 0.0025)
NN(ensemble.size, 0.003)
NN(ensemble.size, 0.0035)
NN(ensemble.size, 0.004)
NN(ensemble.size, 0.005)
NN(ensemble.size, 0.006)

```

```{r}

# build Bayesian network
BN()

```

```{r}

# assess risk
Risk(bn, ensemble.size, sample.size, risk.pool)

```
