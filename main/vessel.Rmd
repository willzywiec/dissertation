---
title : 'vessel'
output : html_document
---

```{r setup, include = FALSE}

knitr::opts_chunk$set(message = FALSE, root.dir = 'C:/Users/Will/Desktop/test', warning = FALSE)

```

# vessel.Rmd
#
# William John Zywiec
# The George Washington University
#
# ...

```{r}

# initialize environment
if (!is.null(dev.list())) dev.off()
rm(list = ls())
cat('\014')

# load packages
library(parallel)

# set source and test directories
source.dir <- 'C:/Users/Will/Dropbox/Dissertation/R/main'
test.dir <- 'C:/Users/Will/Desktop/test'

# set variables
ensemble.size <- 9

sample.size <- 1e+06
risk.pool <- 5

# load functions
source(paste0(source.dir, '/generate.R'))
source(paste0(source.dir, '/nn.R'))
source(paste0(source.dir, '/bn.R'))
source(paste0(source.dir, '/risk.R'))

```

```{r}

# generate data
Generate()

```


```{r}

# build neural network
NN(ensemble.size)

```

```{r}

# build Bayesian network
BN()

```

```{r}

# assess risk
Risk(bn, ensemble.size, sample.size, risk.pool)

```
